services:
  - type: web
    name: indianoil-lucky-draw
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: META_VERIFY_TOKEN
        value: indianoil-verify
